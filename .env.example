# Solo para el modo LOCAL (stdio). NUNCA commitear el .env real.
BEEBOLE_API_KEY=pon-aqui-tu-api-key-de-beebole

# Solo para el modo HTTP (VPS): el token NO va aquí, viaja por cabecera X-Beebole-Key.
# MCP_TRANSPORT=http
# PORT=8087
